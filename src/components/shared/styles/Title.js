import styled from "styled-components";

export const Title = styled.h1`
  color: var(--main-color);
  line-height: 46px;
  font-size: var(--heading-h1);
  font-family: var(--font-headline-regular);
  font-weight: 600;
  text-align: left;
`;
